# Источники данных: архивы, сеть, перенос между ними

Разбор того, готова ли нынешняя архитектура (`TreeProvider`, `TreeEditor` и всё вокруг)
к архивам и сетевым файловым системам, и что в ней стоит поменять раньше, чем
появится второй источник данных.

Сравнение — с тремя менеджерами, у которых это давно работает: Total Commander,
Far Manager, Midnight Commander.

## 1. Что есть сейчас

| Контракт | Гранулярность | Кто реализует |
|---|---|---|
| `TreeProvider` | листинг, разбор пути, разрешение ссылки, размер | `LocalTreeProvider` |
| `TreeEditor` | **операция целиком**: `copy(nodes, dst)`, `move`, `remove`, `makeDirectory` | он же |
| `FilesProvider` | `getFiles`/`putFiles`/`purge` — мост через локальный файл | объявлен, не реализован |
| `NodePath` | цепочка `fs:/a.zip:zip:/inner` | разбор готов, но цепочка не обходится: `PanelController.openPath` берёт из неё только последнюю часть |

Байтового ввода-вывода в контрактах **нет**: провайдер читает каталоги, но не
содержимое файлов.

## 2. Как это устроено у других

**Total Commander** — два вида плагинов. WCX (архивы): `OpenArchive`, `ReadHeader`,
`ProcessFile`; упаковка — `PackFiles(список локальных файлов)`. WFX (файловые системы):
`FsFindFirst`/`FsFindNext`, а передача **всегда** между плагином и локальным файлом —
`FsGetFile(remote, local)`, `FsPutFile(local, remote)`. Прогресс — колбэк `ProgressProc`.

**Far Manager** — панельные плагины: `GetFiles` копирует из плагина в **локальный**
путь, `PutFiles` — обратно. Отдельно стоит флаг `OPIF_REALNAMES`: плагин заявляет, что
отдаёт настоящие имена файлов, и тогда ядро работает с ними своими обычными средствами,
минуя плагин.

**Midnight Commander** — единственный из трёх с настоящим **VFS**: `mc_open`/`mc_read`/
`mc_write`, `mc_opendir`/`readdir`/`stat`, пути цепочкой (`vfs_path_t`:
`/arch.tar/utar://dir/file`). Бэкенды: local, tar/zip (в том числе `extfs`-скриптами),
ftpfs, sftpfs, fish. Копирование — **один цикл** `copy_file_file()` на все бэкенды. Где
бэкенд потока дать не может, `mc_getlocalcopy` скачивает во временный файл.

| Аспект | TC | Far | MC | у нас |
|---|---|---|---|---|
| Архивы | WCX, целиком через локальные файлы | плагины + временная распаковка | VFS, чтение; запись ограниченно | нет |
| Сеть | WFX (плагин ↔ локальный файл) | так же | ftpfs/sftpfs/fish как VFS | нет |
| Любой → любой | двухплечевой через temp | двухплечевой через temp | **потоком**, один цикл | невозможно |
| Прогресс | по плечам, колбэк | по плечам | байты, единый счётчик | по объектам |

Расклад: **TC и Far — файловая гранулярность с мостом через локальную ФС; MC —
потоковая с единым циклом копирования.** Мост нужен обоим подходам, но у MC он
аварийный путь, а у TC и Far — единственный.

## 3. Что у нас сделано правильно

- **Разделение `TreeProvider` / `TreeEditor`.** Источник только для чтения (архив,
  открытый на просмотр) выражается тем, что второй интерфейс не реализован, а не кодами
  ошибок, как у WFX. Это ближе к MC и честнее.
- **`AsyncOperation` с отменой, прогрессом и `OperationRequest`.** Вопрос
  «перезаписать / пропустить / все / отменить» — часть примитива, а не самодельный
  диалог внутри каждого плагина. У всех трёх менеджеров это сделано ad hoc.
- **`NodePath` с цепочкой схем** — готовая основа вложенности, тот же приём, что
  `vfs_path_t` в MC. Разбор уже написан и покрыт тестами; обходить цепочку пока некому —
  `PanelController.openPath` берёт из неё только последнюю часть.
- **Два вида путей** (`pathOf` — видимый, `physicalPathOf` — разрешённый), и второй
  **не входит** в интерфейс: он живёт только в `LocalTreeProvider`, вне него им никто не
  пользуется. Для сетевых провайдеров «физического пути» не существует, и контракт это
  уже допускает.
- **`FilesProvider` предусмотрен заранее** — это в точности `mc_getlocalcopy`.
- **Команды не зависят от источника**: приёмник задаётся строкой пути
  (`TransferCommandBase.destinationParam`), так что «скопировать в `sftp:/host/dir`»
  уже выразимо.

## 4. Что сломается на архивах и сети

**Панель держит один провайдер на всю жизнь.** `PanelController.provider` — `final`, и
оба контроллера получают один экземпляр из контейнера. Войти в архив — значит сменить
провайдера внутри панели; сейчас это негде сделать.

**Межпровайдерному переносу негде жить.** `transfer_commands.dart` разрешает приёмник
провайдером **пассивной** панели, а операцию берёт у редактора **активной**:

```dart
final destination = await _resolveDestination();   // провайдер приёмника
final operation = moves                            // редактор источника
    ? editor.move(targets, destination)
    : editor.copy(targets, destination);
```

Пока провайдер один на обе панели — работает. С двумя разными локальный редактор
получит узел, который не умеет адресовать. Это не предположение, а сегодняшний код.

**Вся механика переноса — внутри провайдера.** Очередь, конфликты, рекурсия, прогресс,
развилка `EXDEV` — в `LocalTreeProvider._transfer`, порядка 110 строк. Второму провайдеру
придётся написать это заново. У MC оно одно на всех, у TC и Far — в ядре, а плагин
двигает один файл.

**Нет байтового контракта** — единый цикл копирования строить не из чего, остаётся
только мост через локальный файл.

**Прогресс в объектах.** Для сети нужны байты, скорость, оценка времени и прогресс
внутри файла; для «архив на FTP → архив на SSH» ещё и по плечам: скачать → упаковать →
загрузить.

## 5. Что менять, по порядку

### 5.1. Вынести перенос из провайдера в ядро

Движок в слое моделей: обход, конфликты, прогресс, выбор стратегии. Провайдеру остаются
примитивы. Стратегии по убыванию скорости:

1. `rename` — один провайдер, умеет (сегодняшний быстрый путь);
2. копирование средствами самого провайдера (серверный `COPY` у FTP, `cp --reflink`);
3. **поток** `openRead → openWrite` — любой в любой;
4. мост через локальный файл, когда одна из сторон потока не даёт.

### 5.2. Байтовый контракт

Отдельным интерфейсом, как `TreeEditor`, — читающий архив его не реализует:

```dart
abstract interface class FileContentProvider {
  AsyncOperation<Stream<List<int>>> openRead(FsNode node, {int offset = 0});
  AsyncOperation<StreamSink<List<int>>> openWrite(DirectoryNode parent, String name, {int? length});
}
```

`offset` здесь не роскошь: без него не будет докачки, а на сети она нужна.

### 5.3. Возможности вместо `provider is TreeEditor`

`canWrite`, `canRename`, `canStream`, `canSeek`, `preservesModified`, `realFileSystem`
(аналог `OPIF_REALNAMES`: позволяет отдать путь внешней утилите и не копировать зря),
`maxConcurrency` — пул обхода каталогов у нас уже есть, и для FTP его придётся
ограничивать по-другому.

### 5.4. Вложенность как композиция

Реестр фабрик по схеме плюс «монтирование» дочернего провайдера над узлом. Здесь стоит
записать неприятную правду: **zip требует произвольного доступа**, поэтому архив на FTP
либо читается range-запросами, либо скачивается во временный файл. MC делает именно
временный файл, и это не лень, а свойство формата.

### 5.5. Прогресс в байтах и по плечам

Расширить `OperationProgress` байтами и текущим файлом, ввести понятие этапа. Скорость и
оценку времени считать в движке, а не в каждом провайдере.

### 5.6. Жизненный цикл и аутентификация

У `TreeProvider` нет ни `connect`, ни `dispose`, ни канала для пароля. Спрашивать пароль
и подтверждать ключ хоста можно уже существующим `OperationRequest`, но сейчас в нём
только выбор из кнопок (`OperationOption`) — нужен вариант с вводом строки.

### 5.7. Уборка временных файлов

Двухплечевая передача обязана убирать за собой при отмене: сессия, владеющая временем
жизни временных копий, и `purge` в `finally`.

## 6. С чего начинать

Пункты 5.1 и 5.2 не требуют ни одного нового провайдера, но снимают главный долг:
сейчас перенос физически невозможно сделать межпровайдерным, и любой второй источник
данных упрётся в это на первой же минуте.

Остальное можно добавлять по мере появления провайдеров: возможности (5.3) — когда
появится источник только для чтения, жизненный цикл и аутентификация (5.6) — когда
появится сетевой.
