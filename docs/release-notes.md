# Что менялось

Выпуски, новыми вперёд. Один выпуск — один тег `vX.Y.Z`: он же собирается на
раннере GitHub (`macos-latest`, arm64) и выкладывается в релизы GitHub.

Пишется **вместе с тегом**, а не после: описание выпуска — это то самое, что
уходит в сообщение тега, и разойтись им незачем. Каждый раздел отвечает на один
вопрос — что человек увидит в приложении нового, — а не перечисляет коммиты: их
и так видно в истории.

Ранние выпуски (`v0.0.1`—`v0.0.15`) описаны сообщением того коммита, на котором
стоит тег: отдельных заметок к ним тогда не писали.

## v0.0.45 — Одна оболочка на командную строку и Ctrl-O

*1 сентября 2026 года*

**Всё, что вы запускали, теперь видно в `Ctrl-O`.** Команда из строки уходит в
ту же оболочку, что открывается по `Ctrl-O`, — раньше у каждой был свой процесс,
и терминал о них ничего не знал. Отсюда и остальное: `export FOO=1` доживает до
следующей команды, `source venv/bin/activate` продолжает действовать, а `.zshrc`
читается раз за сессию, а не на каждый запуск.

Показ вывода остался прежним: `mkdir` не мигает чёрным экраном, молча
провалившаяся команда показывает свой код, долгая молчаливая — свой ход. Чтобы
это уцелело, оболочку просят отмечать каждое приглашение — но не правкой ваших
`.zshrc` или `.bashrc`, как это делают другие: просьба уходит в уже запущенную
сессию и живёт ровно столько, сколько она.

**`cd /tmp && make` теперь уводит и панель.** Строку с продолжением приложение
по-прежнему не разбирает, но куда встала оболочка, оно знает точно.

**Новая настройка:** что делать с экраном, когда команда закончилась, — ждать
клавиши (как было) или убрать сразу. На провалившуюся команду не действует.

**Настройки переехали на `F9`** — туда, где в `mc` меню. `F2` освободился.

**Стрелки в `mc`, `vim` и `less` заработали.** Программы полного экрана просят
терминал переключить курсорные клавиши в особый режим и ждут других
последовательностей; терминал этот режим запоминал, но не применял.

**Командная строка на сервере выполняет команды там, где стоит панель.** Раньше
в оболочку сервера уезжал адрес целиком (`cd ssh://user@host/`), и команда
падала с «No such file or directory».

**Приглашение в терминале и в командной строке набраны одним шрифтом.** Стояли
они на пару пикселей врозь: моноширинное семейство берётся из системы, и на
машине без него подстановку выбирал каждый свою.


## v0.0.44 — Отбивка строк в окнах и мягче углы

*31 августа 2026 года*

**Флажок в окне больше не липнет к строке под собой.** Строка без подписи —
«Follow symlinks» в окнах упаковки и переноса — стоит под полями, а не в их
ряду, и просвет вокруг неё свой, больше обычного. Ставился он только сверху, и
снизу флажок прижимался к соседней строке, читаясь как её часть. Теперь отбит с
обеих сторон.

**Углы окон команд чуть мягче** — пять точек скругления вместо четырёх.


## v0.0.43 — Читаемое окно настроек

*31 августа 2026 года*

**Настройка стала блоком, а не строкой формы.** Подпись, объяснение и
управление идут сверху вниз по одной левой границе. Раньше подпись стояла
справа своего столбца, управление — слева следующего, а объяснение висело под
управлением, и читать приходилось по диагонали. Флаги из этой сетки выпадали
вовсе, а ширину пустого жёлоба слева задавала самая длинная подпись во всём
окне — из-за одной строки пустовал левый край у всех.

**Слева появилось оглавление разделов.** Подсвечен тот, в котором вы сейчас;
щелчок уводит к нему. Прокрутив, больше не теряешься: заголовок раздела стал
крупнее остального текста и говорит, чьи это настройки.

**Сверху — поиск.** Ищет по подписи, названию раздела, объяснению и ключу;
раздел, в котором ничего не совпало, пропадает и из списка, и из оглавления.
Найденное выделено, рядом с полем — сколько нашлось.

**Видно, что вы меняли.** У настройки, стоящей не на умолчании, слева полоса, а
рядом с подписью — «Reset». Умолчание теперь называет сама настройка, а не
прячет в себе раздел настроек, и тест следит, чтобы эти двое не разошлись.

**Настройка «Directory size scans» не сохранялась вовсе** — с самого появления
окна настроек. Правка уходила в копию состояния и пропадала, не пережив даже
закрытия окна. Теперь сохраняется и действует сразу, без перезапуска.

**Выбор одного из нескольких — выпадающим списком.** Тема и степень сжатия в
окнах создания архивов занимали столько строк, сколько у них вариантов. Список
выглядит как поле ввода, раскрывается под ним и по его ширине.


## v0.0.42 — Составные расширения

*31 августа 2026 года*

**`archive.tar.gz` — это архив `tar.gz`.** Раньше в колонке имени стояло
`archive.tar`, а в колонке расширения — `gz`; то же самое случалось с
`button.spec.ts` и `jquery.min.js`. Теперь имя и расширение делятся так, как их
видит человек, и сортировка по расширению держит составные вместе, а не
рассыпает их между `gz` и `bz2`.

Составные расширения берутся из словаря, а не угадываются по числу точек:
правило «две точки подряд» превратило бы `readme.v2.txt` в файл `readme` с
расширением `v2.txt`. Встроенный список короткий нарочно — то, что встречается
каждый день: `tar.gz`, `tar.bz2`, `tar.xz`, `tar.zst`, `tar.lz4`, `spec.ts`,
`test.ts`, `d.ts`, `min.js`, `min.css`, `min.mjs`, `blade.php`. Своё дописывается
в настройках через `;` и стоит впереди встроенного; встроенный список можно
выключить целиком.

Правка словаря видна сразу, без перезапуска. А если выключить колонку
расширения — правой кнопкой по заголовку, — имена показываются целиком, как и
раньше.

Открытия файлов это не касается: `a.tar.gz` по-прежнему открывается как архив
`gz`, внутри которого лежит `tar`. Словарь отвечает на вопрос «как показать», а
не «что это за файл», и те, кто выбирает просмотрщик или монтирует архив, его не
видят.

## v0.0.41 — История адресов свежим вперёд, размеры — числами

*31 августа 2026 года*

**Последний открытый адрес стоит в истории первым.** `Cmd-F1` и `Cmd-F2`
показывали список по алфавиту, хотя хранился он правильно — свежим вперёд.
Адрес, за которым в это окно чаще всего и приходят, оказывался где придётся.

Остальное в этом выпуске — внутреннее, и на вид приложения не влияет.

**Размеры оформления стоят числами.** Раньше они считались из исходников
референса общим множителем, и половина набора читалась через умножение в уме, а
половина — как есть. За разные dpi отвечает Flutter, а не мы, так что пользы в
этом не было, а тому, кто пишет свою тему, приходилось держать множитель в
голове. Вид не изменился ни на пиксель.

**Платформенное живёт отдельно** — пакетом `fc_platform`: окно, буфер обмена,
запуск программ, псевдотерминал, открытие файла системой, временные каталоги.
Раньше всё это лежало в модуле локальной файловой системы, к которой не имеет
отношения. Когда дойдут руки до второй операционной системы, менять придётся
ровно один пакет.

## v0.0.40 — Правка системных файлов, своих и на сервере

*31 августа 2026 года*

Файл, который принадлежит не вам, теперь всё-таки сохраняется. `/etc/hosts` у
себя, `/etc/squid/squid.conf` на сервере — приложение спрашивает согласие, при
необходимости пароль, и пишет от администратора. Повышается при этом **одно
действие**, а не весь сеанс: панели, терминал и всё остальное остаются вашими.

**Редактор перестал молча делать необратимое.** `F2` спрашивает перед записью —
она соседствует с `F3` и `F5`, а отмены у неё нет. Выход с несохранённым даёт
три ответа вместо двух, и `Enter` теперь сохраняет, а не теряет: раньше самое
частое «да, я закончил» стирало работу. Не записалось — экран остаётся
открытым, и ошибка видна там же.

**Права видно до правки, а не после часа работы.** Файл, в который писать не
дадут, встречает вопросом: открыть на чтение или править всё равно и сохранить
от администратора. Раньше об отказе узнавали на `F2`, когда деть текст уже
некуда.

**Права файла переживают сохранение.** Файл с режимом `600` после правки
оставался `644`: запись подменяла его новым, а тот приходил со своими правами
по умолчанию.

**Оболочка на той стороне `ssh`.** Панель стоит на сервере — командная строка
внизу работает, и набранное выполняется **там**, в том каталоге, который
показывает панель. `Ctrl-O` разворачивает оболочку того же сервера; их может
быть несколько, по одной на сервер, и все живут одновременно. Второго входа и
второго пароля не спрашивается: терминал берёт соединение, которое открыла
панель. Приглашение показывает, где выполнится набранное, — `rm` на сервере не
спутать с `rm` у себя.

**Открытие файла с сервера перестало выглядеть зависанием.** Пока `F3` или `F4`
читают файл, панель говорит об этом строкой состояния, а `Esc` чтение бросает.
Раньше нажатие выглядело потерянным: курсор ходил, клавиши работали, и через
миг поверх всего вставал экран с тем файлом, который был под курсором **тогда**.

**С занятой панели теперь можно уйти.** `Tab` уводит на соседнюю, и там всё
работает — занятость это свойство панели, а не приложения. Копировать в занятую
панель при этом нельзя: она сама сейчас читает.

**Окно открытия адреса застывает на время работы.** Пока идёт подключение, по
истории не походить и другого адреса не набрать: окно показывало одно, а
открывалось то, что запустили.

Что осталось как было: повышение можно выключить целиком — «Allow elevated
writes» в настройках; выключенное, оно не предлагается вовсе, и отказ остаётся
отказом. Пароль живёт только в памяти сеанса и помнится по машинам порознь.

## v0.0.39 — Палитра: описания и страницы

*30 августа 2026 года*

Палитра команд (Cmd-Shift-P) стала отвечать на вопрос «а что эта команда
делает». Справа от названия теперь стоит описание команды — то же, что в
справке, — а не название модуля: модуль сообщал ровно то, что и так видно по
названию.

**Окно стало шире** — три четверти экрана вместо половины: в строке теперь
помещаются название, описание и клавиши разом, и описание не обрывается
многоточием на первом же слове.

**PgUp и PgDn листают список страницами** — по числу видимых строк минус одна,
как в панели: строка перекрытия не даёт потерять место, где остановился взгляд.
У края страница упирается, а не заворачивается в другой конец списка. Те же
клавиши заработали и в истории адресов (Cmd-F1 / Cmd-F2), и в окне пометки по
маске: список там один и тот же, и расходиться повадкой ему незачем.

**Искать по названию модуля по-прежнему можно**, хотя видно его больше нигде:
`term` находит команды терминала. Модуль переехал к синонимам — к невидимым
словам, по которым команда отзывается, — и весит там столько же, сколько весил.

Само описание при этом не ищется: предложение состоит по большей части из общих
слов, и поиск по нему выдавал бы всё сразу на любом «file» или «panel».

Что осталось как было: у команд, которым объяснять нечего — «Cursor up», «Page
down», — описания нет вовсе, и в списке они стоят одним названием; стрелки в
палитре по-прежнему ходят по кругу, а страницы — нет; остальные окна приложения
ширины не поменяли.

## v0.0.38 — Пометка мышью

*30 августа 2026 года*

Правая кнопка теперь помечает. Щелчок переключает пометку строки,
протягивание помечает всё, через что провели, — повадка Norton и Total
Commander и самый быстрый способ набрать группу файлов, не берясь за
клавиатуру.

**Отрезок, а не след.** Жест помнит строку, с которой начался, и приводит к
одному виду весь отрезок от неё до текущей. Поэтому ход назад снимает то, что
жест сам же и пометил: протянули до десятой строки, вернулись к пятой — с
шестой по десятую всё как было. Направление задаёт первая строка: начали с
непомеченной — помечаем, с помеченной — снимаем.

**У краёв список едет сам**, и тем быстрее, чем дальше указатель ушёл за край.
Едет он по таймеру, а не по движениям мыши: остановленная за краем рука список
не останавливает — иначе пометить остаток длинного каталога можно было бы
только дрожанием руки.

Курсор идёт за жестом и встаёт на последнюю задетую строку, панель становится
активной, «..» не помечается ни при каком направлении.

Что осталось как было: над заголовками колонок правая кнопка по-прежнему
открывает меню видимости — жест начинается только на строке списка;
перетаскивание файлов остаётся за левой кнопкой; клавиатурная пометка
(`Space`, `+`/`-`, `Cmd-A`) не изменилась ни в чём. На трекпаде протянуть с
зажатой правой физически невозможно, и подпорок для этого нет: двухпальцевый
тап переключает пометку одной строки, а всё остальное делают клавиши.

## v0.0.37 — Пометка по маске

*30 августа 2026 года*

Пометить полсотни `*.dart` можно было только полусотней нажатий `Space` —
теперь это `+` и одна строка. `-` снимает пометку по той же маске.

**Маска — это `*`, `?` и точка с запятой.** `*.dart;*.md` — два образца,
`*;!*.bak` — «всё, кроме резервных копий»: образец с `!` исключает, и
исключения применяются последними, независимо от порядка в строке. Регистр не
важен: `*.PNG` и `*.png` — одно и то же. Маска смотрит на имя целиком, поэтому
`*.d` пометит и каталог `src.d`, а `*.gitignore` — файл `.gitignore`.

**Пометка дополняется, а не заменяется**: `+` дважды с разными масками помечает
и то, и другое. «..» не помечается никакой маской — это не объект, а выход
наверх.

**Сколько совпало, видно до нажатия.** Под полем идёт счётчик — «12 of 40», —
и он меняется, пока набираешь: единственное место, где маска молчала бы до
самого `Enter`, а ошибиться в ней легко. Пока маска не набрана, там приглушённое
«No files selected».

**Недавние маски — под полем**, как в окне адреса: набранное отбирает, `↓`
вписывает выбранное, `Enter` применяет. Помнятся два десятка последних и
переживают перезапуск.

На маке `+` набирается как `Shift-=`, поэтому у пометки две клавиши: она и `+`
цифрового блока. В справке пишется `+` — то, что человек нажимает. Пока снимать
нечего, `-` остаётся за переходом к имени: минус в имени файла встречается чаще,
чем желание снять несуществующую пометку. В режиме `mc` оба символа, как и
любые другие, набираются в командной строке.

Движок масок лежит в `fc_api` и ждёт следующих: окраска строк по правилам и
поиск файлов будут понимать `*.txt` ровно так же.

## v0.0.36 — Переименование по Shift-F6

*30 августа 2026 года*

Переименования в приложении не было вовсе — ни клавишей, ни в списке команд.
Теперь `Shift-F6` меняет имя объекта под курсором: окно с одним полем, имя уже
в нём, а выделена **основа без расширения** — правят обычно её, и `.tar.gz`
дописывать заново не приходится. Ряд кнопок показывает «Rename» на `F6` сам,
стоит зажать `Shift`.

**Занятое имя отказывает, а не спрашивает.** Согласие здесь означало бы потерю
чужого файла, о котором человек мог и не знать: переименование в системе молча
затирает то, что попалось под новым именем. Окно при этом не закрывается — имя
правят там же. Смена регистра (`readme.md` → `README.md`) занятым именем не
считается: это тот же самый файл.

**Пробелы по краям срезаются сами** — и по краям всего имени, и по краям
основы: `«  отчёт  .txt  »` сохранится как `отчёт.txt`. Хвостовой пробел перед
точкой не виден ни в поле, ни в списке, а файл получается другой. То же правило
теперь и у `F7`.

В архиве переименования нет: там оно означало бы пересборку архива целиком, а о
такой работе полагается спрашивать заранее — этот вопрос стоит завести
отдельно и осознанно.

**Заодно: после файловой работы обновляются обе панели**, если обе смотрят в
один каталог. Раньше перечитывалась только та, в которой работали, — и вторая
показывала прежнее имя, отсутствующий каталог или уже удалённый файл. Касается
переименования, создания каталога, удаления и переноса.

## v0.0.35 — Файл из архива читается потоком

*29 августа 2026 года*

Открыть файл из `.zip` раньше значило распаковать его целиком в память — и
сделать это в том же изоляте, который рисует окно. Четырёхгигабайтный файл не
открывался вовсе, гигабайт морозил приложение, а отмена отменяла только запись:
распаковка к тому времени уже случилась.

Теперь сжатые байты берутся прямо из файла архива, а разжимает их системный
zlib — **по мере того, как их просят**. Память ровная на файле любого размера;
медленный приёмник (сеть, архив на сервере) останавливает и распаковку, а не
копит распакованное в памяти; отмена прекращает работу, а не ждёт её конца.
Быстрее вышло заодно: системный zlib проворнее того, что было.

Видно это везде, где содержимое читается: просмотр, копирование из архива,
перетаскивание файла наружу.

Зашифрованные записи и редкие способы сжатия читаются по-прежнему — там
расшифровку делает библиотека, и трогать её ради этого не стоило. `tar` и `7z`
не менялись: первый и так отдаёт поток из файла, второй — из внешней программы.

## v0.0.34 — В архив на сервере теперь можно писать

*29 августа 2026 года*

Копировать в `.zip`, лежащий на сервере, раньше было нельзя — ни `F5`, ни
мышью. Причина была не в сети: архив не на диске открывается через временную
копию (оглавление zip лежит в конце файла, и прыгать по нему можно только там,
где файл есть), а писать в копию, которую некому вернуть, бессмысленно.

Теперь есть кому: **пересобранный архив уезжает обратно хозяину**. Заливается он
рядом, под временным именем, и встаёт на место оригинала одним движением — так
что обрыв связи оставляет прежний архив, а не обрубок. Сорвалось — предложат
повторить: пересобранный файл уже готов, и терять его из-за одной осечки
незачем.

Условие «в архив можно писать» перестало зависеть от того, где он лежит, и
стало зависеть от того, **что умеет его хозяин**: принять содержимое и
переименовать. У архива внутри архива хозяин — `zip`, а переименовывать он не
умеет вовсе, поэтому вложенный архив остаётся только для чтения — как и был, но
теперь по осмысленной причине.

**О полной пересборке предупреждают заранее.** Дописать запись в zip нельзя:
добавление одного файла переписывает архив целиком, а лежащий на сервере ещё и
уезжает обратно — с размером в вопросе. Знать это по счётчику байт, когда работа
уже пошла, поздно; вопрос задаётся до начала и для архива на диске тоже.

Отправка идёт вторым плечом работы, следом за пересборкой: с полосой, скоростью
и отменой.

Заодно починено то, что нашлось по дороге: файл из Finder, брошенный в архив на
сервере, ронял работу. Путь-источник разбирала панель-приёмник — то есть искала
его на сервере.

## v0.0.33 — Наружу тащится и то, чего нет на диске

*29 августа 2026 года*

Файл из архива или с сервера можно перетащить наружу — в Finder, в заметки, в
почту, куда угодно. Настоящего пути у него нет, поэтому наружу уходит
**обещание**: приложение-приёмник получает имя и тип сразу, а содержимое — когда
и если попросит.

**До этой просьбы не читается ничего.** Передумать по дороге ничего не стоит:
архив не распаковывается, сеть не тревожится. А когда попросят, файл пишется
**сразу туда, куда просит приёмник** — без промежуточной копии, то есть один
проход по диску вместо двух. Куда именно, решает приёмник: Finder называет папку,
куда бросили, а заметки или мессенджер — свой временный каталог, из которого
втянут содержимое к себе.

Долгая выкладка идёт обычной работой: полоса под панелью-источником, отмена,
удаление недописанного файла при срыве. А источник — архив или соединение —
держится живым ровно до того мгновения, когда содержимое прочитано: можно
перетащить файл и, не дожидаясь, уйти из архива.

Каталоги из архивов пока не перетаскиваются: выложить каталог значит обойти его
целиком, и это отдельная работа.

## v0.0.32 — Перетаскивание мышью

*28 августа 2026 года*

Файлы можно таскать мышью — из Finder в панель, из панели в Finder и между
панелями.

**Из системы.** Брошенное ложится в каталог под курсором, если целились в него,
иначе в тот, что открыт в панели; пока держите — цель обведена. Бросок при этом
**не начинает работу сам**: он открывает то же окно, что `F5` и `F6`, с уже
заполненным приёмником — и вы ещё успеете поменять его, включить проход по
ссылкам или передумать. `Shift` при броске означает перенос вместо копии; решает
это система, она же рисует привычный значок у курсора.

**Наружу.** Строку можно утащить в Finder или в любое чужое окно: за курсором
едут системные значки файлов, а помеченное едет целиком. Наружу отдаётся только
копия — переносить значило бы удалять исходное по сообщению от чужого
приложения. Из архивов и с серверов пока не перетаскивается: там нужны
«обещанные» файлы, и это следующая работа.

**Панель себе приёмником не бывает:** бросить объекты туда, где они и лежат,
значит не сделать ничего, и приложение не притворяется, что сделает. Кому нужно
обратное — перетащить в подкаталог, не уходя из панели, — включает это одной
настройкой.

Всё это написано своей нативной частью в раннере, а не плагином: сборка macOS
остаётся без CocoaPods. Заодно первый щелчок по окну после перетаскивания
перестал пропадать — и тот, что возвращает приложение из фона, тоже.

## v0.0.31 — Ответы принадлежат тому, кто спрашивает

*28 августа 2026 года*

В приложении не меняется ничего — этот выпуск про порядок внутри и про то, чем
собираются остальные.

**Варианты ответа на вопрос работы** («перезаписать», «пропустить все»,
«прервать») больше не лежат общим списком в самом типе вопроса. Тип вопроса —
это значение: имя для кода и подпись для кнопки; а «перезаписать» — словарь
переноса файлов, и живёт он теперь рядом с движком переноса. Подтверждение
отмены — рядом с работой, которая его задаёт. Модуль со своим вопросом
объявляет свои варианты и ни у кого не спрашивается; окно, как и раньше, не
знает по имени ни одного — рисует те, что дали. Заодно ушёл вариант, которым не
пользовался никто.

**Сборка переехала на раннеры GitHub** со своего агента: SDK ставится
действием с кэшем и прибит к рабочей версии, чтобы прогон не ломался и не
чинился от чужого обновления.

## v0.0.30 — Флаги в окнах стоят под полями

*28 августа 2026 года*

Строка без подписи — флаг, сообщение об ошибке, полоса хода работы —
начинается там же, где значения, а не у левого края окна. Так в референсе, и
причина простая: флаг под полем ввода читается как продолжение того поля, к
которому относится, а прижатый к краю — как что-то отдельное от формы.

Просвет над такой строкой стал больше обычного зазора между полями (двенадцать
точек против восьми) и, как всё в оформлении, зовётся по имени —
`dialogWideRowGap`. С одинаковым просветом строка читалась как ещё одно поле, у
которого забыли подпись.

## v0.0.29 — Enter запускает программы

*28 августа 2026 года*

`Enter` на файле с битом `+x` выполняет его — во внутреннем терминале, той же
дорогой, что и набранное в командной строке: тот же каталог, та же оболочка, тот
же экран с выводом. Порядок разбора `Enter` в панели складывается сам собой из
объявления модулей и выполнимости команд: сперва командная строка, потом
исполняемый файл, потом обычное открытие. Настройка `runExecutables` (включена)
возвращает `Enter` системе тем, кому привычнее так.

Три находки живой проверки терминала, и первая — та, из-за которой из `cat` было
не выйти вовсе. У запущенной программы **не было управляющего терминала**:
`^C` печатался, а сигнал слать было некому — группы переднего плана не
существовало. Из-под `bash` то же самое работало, из-под `zsh` — нет. Теперь
между запуском и программой на один вызов встаёт `/bin/sh`: он открывает
терминал изнутри сессии и заменяет себя программой. Заодно заработали `Ctrl-Z`,
управление заданиями и `/dev/tty`.

`Ctrl-O` над работающей командой больше не заводит второй полноэкранный
терминал, а убирает её с глаз и той же клавишей возвращает: процесс при этом
продолжает работать. После закрытия экрана команды курсор больше не пропадает:
командная строка при сборке спрашивает, за ней ли ввод.

`Ctrl-C` сам убирает экран прерванной команды. Раньше программа умирала сразу, а
экран оставался ждать клавиши — по общему правилу «что-то вывела», а прерванная
почти всегда что-то вывела, хотя бы `^C`. Выходил ритуал из двух нажатий, и со
стороны это читалось как «`Ctrl-C` не сработал».

**Список файлов набирается системным шрифтом.** Consolas приложение больше не
возит с собой: он принадлежит Microsoft и лицензируется только вместе с тем
продуктом, в котором поставлен. Имя роли осталось прежним — у кого стоит Office
или Windows, тот видит ровно то же, что и раньше, — а у кого нет, тому
подставляется системный моноширинный (на macOS Menlo). Подстановка названа
явно, потому что иначе её делает система и берёт свой обычный пропорциональный
шрифт: столбцы размеров и дат от такого разъезжаются. По высоте строки Menlo
совпадает с Consolas точь-в-точь, так что сетка списка не поехала; знак у него
шире на десятую долю, и колонка имён вмещает примерно на девять процентов
меньше символов.

## v0.0.28 — Окно без системной полосы заголовка

*28 августа 2026 года*

Приложение занимает окно целиком: светлой системной полосы над ним больше нет,
фон вверху такой же, как везде. Вместо неё — своя полоса: за неё окно двигают,
двойное нажатие разворачивает и возвращает обратно. Высота — величина темы
(`windowTitleBarHeight`, двадцать восемь точек: ровно системная полоса macOS,
чтобы светофор стоял на привычном месте).

Светофор остался: закрытие, сворачивание и разворот — привычки системы, и
подменять их своими кнопками незачем. Осталось и всё остальное, что система
делает хорошо, — тень, скругление, изменение размера за края и притягивание к
краям экрана. Забирается ровно то, что мы делаем лучше неё: пустая полоса
чужого цвета. Полоса пока пустая и у нас — со временем туда встанут вкладки
панелей.

Линейки в файловой панели больше не сходятся с рамкой: разделители колонок и
линия над строкой состояния не доходят до края на толщину обводки — как в
референсе, где рамка читается рамкой, а не продолжением сетки.

## v0.0.27 — Рамка окна: скругление, поля и высота строки состояния

*28 августа 2026 года*

Панели скруглены по углам, и это величина темы, а не число в вёрстке
(`panelRadius`, пять точек — столько же, сколько у полей окна, так что рамка
с отступом читается как одно целое). Скругление появляется вместе с полями: у
панели, прижатой к самому краю окна, внешняя сторона не рисуется вовсе, и
скруглять там нечего.

Строка состояния внизу панели стала выше — двадцать восемь точек вместо
двадцати четырёх, на которых текст сидел впритык.

Ряд функциональных кнопок стоит ближе к краям окна, чем панели над ним: он
рисованная клавиатура, и общая рамка ему ни к чему. Раньше выходило иначе и
несимметрично — слева поле приходило от окна, а справа к нему прибавлялось
собственное поле ряда из референса; теперь у ряда свой выступ, и с обеих
сторон одинаково.

Все три — величины темы: захотелось плотнее или свободнее, крутится одним
числом в одном месте.

## v0.0.26 — Сведения об объекте

*28 августа 2026 года*

Cmd-I и Alt-Enter показывают всё, что известно о файле: имя, полный путь, тип,
размер, даты, права, куда ведёт ссылка и откуда он открыт — с диска, из архива
или с сервера. У каталога размер не считается сам: для этого кнопка, потому
что открытие сведений о корне не должно означать обход диска. Помеченное даёт
сводку, а не окно на каждый файл.

Содержимое окна приносят провайдеры, объявленные модулями, — само оно не знает
о файлах ничего. Просмотрщик картинок уже добавил свой раздел: размеры и
формат. Дальше туда встанут метод сжатия от архиваторов, расширенные атрибуты
от правки атрибутов, владелец и группа. Провайдер, который объявил, что читает
такой формат, и не справился, так и говорит — это сведение о файле, а не
жалоба приложения на себя.

Те же сведения показываются вместо мусора там, где файл открыть нечем: F3 на
.bin даёт имя, размер и даты, а быстрый просмотр перестал отговариваться
словом «Directory» — под курсором каталог, и о нём же рассказывают.

Заодно окна перестали быть фиксированной ширины: справка, настройки и окна
команд растут по содержимому, но не шире трёх четвертей окна приложения — под
ними всегда остаётся видимый край панелей. А справка стала настоящей таблицей:
столбцы совпадают во всех разделах, заголовки идут во всю ширину, и длинное
описание переносится по краю окна, а не посередине пустого места.

## v0.0.25 — Картинки

*28 августа 2026 года*

F3 открывает изображения: png, jpeg, gif, webp и bmp. В архиве и на сервере
так же, как на диске, — байты приходят тем же способом, каким копируются
файлы. Быстрый просмотр показывает их рядом со списком и меняет на шаг
курсора.

Вписать в окно или точка в точку (F2), масштаб на «+» и «−» или колесом с
Cmd, прокрутка колесом и мышью. Стрелки во весь экран листают соседние
картинки каталога, не сходя с показа; в панели они по-прежнему принадлежат
курсору — просмотр рядом со списком следует за ним, и только за ним.

Память бережёт не один предел, а два: объём файла и число точек. Второй
нужен потому, что первый о памяти ничего не говорит — сжатая картинка
разворачивается в четыре байта на точку, и пять мегабайт бывают полутора
гигабайтами. Оба читаются из заголовка, до распаковки; больше предела —
приложение говорит словами и показывает, чем открыть (Cmd-O).

Всё это принёс отдельный модуль, а в самом просмотре не изменилось ни
строки: реестр просмотрщиков, заведённый прошлым выпуском, отработал ровно
так, как задумывался. Следующий просмотрщик встанет так же.

Заодно: сплошному содержимому отдана вся рама панели — картинка занимает её
целиком, а плашка с путём лежит поверх. И плашка перестала вылезать за края
в узкой панели.

## v0.0.24 — Поля по краям окна

*27 августа 2026 года*

Содержимое больше не упирается в границы окна: слева и справа у него
появился воздух — такой же, какой был сверху и снизу. Отступают все трое
разом: панели, командная строка и ряд кнопок, — иначе кнопки выпирали бы
за края того, что подписывают.

Рамка панели при этом замыкается со всех четырёх сторон. Раньше внешний
край не рисовался потому, что он и был краем окна: отделять там не от
чего. Теперь край перестал быть краем, и открытая сторона в отступе
выглядела бы недоделкой.

Величина живёт в теме (windowSidePadding) рядом с зазором между панелями,
и по умолчанию это пять точек: захочется плотнее или свободнее — крутится
одним числом, и разъезжаться нечему.

## v0.0.23 — Реестр просмотрщиков и окна, которые можно отодвинуть

*27 августа 2026 года*

F3 больше не привязан к тексту: чем показывать файл, решает реестр.
Просмотрщик объявляет себя модулем — какие файлы берёт и что открывает, — а
оболочка выбирает подходящего по приоритету. Снаружи сегодня ничего не
меняется: просмотрщик пока один, текстовый. Зато следующий — картинки —
появится сразу в обоих местах, и на F3, и в быстром просмотре, без единой
правки в них самих.

Показ текста переехал в свой модуль. Выключите оболочку — просмотра не
будет вовсе; выключите текст — F3 останется и скажет словами, что этот файл
показать нечем. Из-за переезда раздел настроек просмотрщика сменил имя:
перенос строк, номера строк и предел размера один раз вернутся к
умолчаниям.

Окно команды теперь можно отодвинуть за полосу заголовка — посмотреть, что
под ним: список файлов во время копирования, ошибку в панели, имя, которое
хотелось списать. Совсем за край окно не уходит.

Мелочи, заметные каждый день: разделитель панелей ловится мышью там, где
обещан, — раньше область захвата была ровно в ширину зазора; отступы внизу
окна перестали спорить друг с другом, и командная строка стоит на равном
расстоянии от панелей и от ряда кнопок.

## v0.0.22 — Быстрый просмотр в соседней панели

*27 августа 2026 года*

Shift-F3 — соседняя панель показывает то, что под курсором, и хождение по
списку меняет показанное. Каталог логов или конфигов теперь читается стрелкой
вниз, без пары «F3 — Esc» на каждый файл. Пауза перед чтением ждёт, пока
перебор остановится: читается тот файл, на котором остановились, а начатое
чтение бросают на полпути.

Tab уводит ввод в просмотр — и там работают все клавиши просмотрщика: перенос
строк, номера, поиск, копирование выделенного, стрелки листают текст. Tab
возвращает ввод файлам, Esc убирает показ. Панель под ним цела: тот же каталог,
курсор и пометка, — а копировать в панель, показывающую файл, приложение больше
не предлагает: панели там сейчас нет.

Под курсором каталог, слишком большой файл или ошибка — причина сказана словами
в самой панели, а не всплывающим сообщением на каждом шаге курсора.

Видно и то, куда ушёл ввод: ярко горит плашка того, кому достаются клавиши, —
у файлов или у показа. Раньше горели обе.

Заодно в командной строке: длинный путь больше не режется многоточием при
пустом поле — приглашение берёт по содержимому, а поле оставляет себе треть
строки. И фон у строки и у полноэкранного терминала теперь оконный: они
показывались чуть светлее общего фона, потому что брали цвет панели.

## v0.0.21 — Архивы tar, gz и tar.gz, а к ним синонимы в палитре

*27 августа 2026 года*

.tar, .gz, .tar.gz и .tgz открываются входом внутрь, как каталог. Провайдера
два, потому что и форматов два: tar — контейнер без сжатия, gz — сжатие
одного потока, в котором имён нет вовсе. .tar.gz из-за этого получается сам
собой, цепочкой, и особого случая для двойного расширения в приложении нет.

Разжимается такой архив ровно один раз: gz отдаёт содержимое потоком и
временных файлов не заводит, а на диск его выкладывает тот, кому нужен
настоящий файл. Внутри tar видны права и симлинки — то, ради чего мир Unix
за него и держится; ссылки переносятся ссылками.

Паковать умеет «Mk Tar»: .tar, .tar.gz или .tgz, с выбором, идти ли по
ссылкам. Архив собирается потоком, и у .tar.gz промежуточного несжатого
файла не появляется вовсе. Отдельная «Mk Gz» сжимает один файл: gzip жмёт
поток, а не набор файлов, и просьбу сложить в один .gz три файла формат не
выполняет.

Клавиш обеим не досталось — Shift-F5 занял zip, Shift-F7 — 7z, — и они
живут в палитре. Ради этого палитра научилась искать по синонимам: «Mk Tar»
умеет .tar.gz, а на запрос gz не отзывалась вовсе. Теперь упаковщики
находятся по archive, compress и pack, «Mk Dir» — по folder, «Settings» —
по preferences, «Reload» — по refresh, «Terminal» — по shell, и так ещё у
полутора десятков команд.

## v0.0.20 — Палитра команд, размеры каталогов, история адресов

*27 августа 2026 года*

Палитра (Cmd-Shift-P) показывает всё, что приложение умеет сейчас, с
нечётким поиском по названию и модулю; недавние идут первыми. Окно без
кнопок — поле и список, высотой в целое число строк.

Alt-Shift-Enter считает размеры всех каталогов панели: та же очередь, что
и у пометки, посчитанное лежит в самих узлах.

В окне адреса (Cmd-F1/F2) под полем — история того, куда уже успешно
ходили, с тем же поиском, что и в палитре. Enter открывает то, что в
поле; стрелка вписывает выбранное в него. Пароль в историю не попадает,
предел истории — настройка.

Тем же выпуском вышло **окно настроек** (F2 и Cmd-,): всё, что человек
выбирает, — в одном месте, разделами по модулям; меню на F2 убрано совсем.
В сообщении тега оно не названо — работа лежала в ветке, влитой заодно с
остальными.

Заодно: сортировка по заголовку колонки снова работает — живой клик
сдвигает указатель на точку-другую, и его забирало себе перетаскивание
колонок. И командной строки больше не видно под полноэкранным терминалом.

## v0.0.19 — Раздел настроек — того модуля, который его просит

*26 августа 2026 года*

Найдено в файле настроек живого приложения: раздел fc.editor содержал
ключи терминала — shell, maxLines, typingGoesToLine.

Имя раздела вычисляется по тому, чья установка идёт сейчас. Терминал,
просмотрщик и редактор просили область лениво — из замыкания, уже после
установки, — и получали последний установленный модуль. Пока терминал стоял
в списке последним, всё сходилось случайно; перестановка ради режима mc это
вскрыла.

Теперь область забирается один раз, в install, — как и написано в
modules.md для локальной ФС. А спрошенная после установки бросает: чей это
раздел, в тот момент уже неизвестно, и молча отдавать чужой хуже, чем
отказать.

Тест собирает настоящее приложение и смотрит, где лежат ключи. Без правки он
падает.

## v0.0.18 — Дополнение путей в командной строке

*26 августа 2026 года*

Tab дополняет путь по началу имени: один кандидат вставляется целиком, из
многих дописывается общее начало, дальше перебор по кругу, а над строкой
видно, из чего выбирать. Пока идёт выбор, Enter закрепляет подставленное, а
не выполняет команду — по пути погружаются, а не запускают его; Esc
возвращает набранное руками.

Заодно: изолят чтения псевдотерминала больше не запирает перезапуск
приложения.

## v0.0.17 — Терминал: клавиатура, окружение и мелочи

*26 августа 2026 года*

Клавиатура в полноэкранном терминале работает целиком: Backspace, стрелки,
Ctrl-C и любая раскладка. Оболочка получает окружение приложения плюс TERM и
UTF-8-локаль и запускается как login — иначе не находилось ничего из
Homebrew.

Плюс: отказ в доступе больше не стирает панель, пробел при вставке имени
ставится только между словом и именем, отступ строки состояния взят из ролей
темы.

## v0.0.16 — Терминал и командная строка

*26 августа 2026 года*

Оболочка в том же окне: строка под панелями и полноэкранная сессия по
Ctrl-O, как в mc. Cmd-T отдаёт строке ввод, Esc возвращает панели, cd ведёт
панель. Enter запускает команду своим процессом в каталоге панели — экран
показывается только когда есть что показать.

Псевдотерминал сделан на dart:ffi: posix_openpt и posix_spawn из libSystem,
без нативной части и без CocoaPods в сборке macOS.

## v0.0.15 — Ссылка переносится ссылкой, а не вопросом

*26 августа 2026 года*

Копирование /etc/nginx с сервера упиралось в вопрос на каждом симлинке:
ссылку умел создать только тот провайдер, у которого её взяли, — copyEntry
это «сделай копию у себя». Между источником и приёмником сказать «заведи
ссылку вот на это» было нечем.

Теперь есть LinkEditor — отдельный интерфейс рядом с FileContentReceiver.
Менять дерево и заводить ссылки — разные умения: архиву ссылки не нужны,
Windows пускает не всякого. Про умение говорит наличие интерфейса, про
отказ по ходу дела — FsError, и тогда остаётся прежний вопрос.

Умеют локальная ФС (Link.create) и SFTP (SftpClient.link, порядок доводов
там обратен названиям — проверено на живом). Архивы не объявляют ничего:
ссылку в архив кладёт сам архиватор своей записью.

Цель переносится строкой как есть: относительная остаётся относительной.
Битая ссылка переносится и остаётся битой — ln -s заводит такие же.

Живьём: /etc/nginx с сервера на диск, ноль вопросов, шесть ссылок с теми
же целями, 3,6 с на всё дерево.

## v0.0.14 — Набросок __ref удалён: переход закончен

*25 августа 2026 года*

Он был проектом целевого API на время перехода. Всё, что в нём лежало,
либо переехало в код, либо оказалось не наступившей надобностью.

Последними ушли двое. Strings слово в слово лежит в roadmap.md, Б1 —
второй копии ему не нужно. showAttention переехал в §13 «за рамками»:
поднять окно работы, ждущей человека, откуда угодно понадобится, когда
спрашивать начнёт модуль, к команде отношения не имеющий, — вход в
зашифрованный архив, которого ждёт панель. Сегодня хватает того, что окно
возвращает тот, кто работу завёл.

Каталог лежал в lib/ и собирался вместе с пакетом, ничего не экспортируя:
для наброска это было уместно, для законченного перехода — мёртвый вес.

## v0.0.13 — Листание проверено на живом

*21 августа 2026 года*

Отметка в спецификации: `F3` на большом файле, собранное приложение. Тесты
меряют положение прокрутки, но глазами на это не смотрели, — теперь посмотрели.

Из невыполненного по спецификации остаётся только отправка обеих правок наверх,
в апстрим.

## v0.0.12 — Варианты переключателя разделены общим зазором окна

*20 августа 2026 года*

Между ними стоял checkboxGap — а он про другое: про расстояние от знака до
его метки, внутри одного варианта. В горизонтальном ряду это склеивало
варианты друг с другом.

Теперь тот же dialogGap, что разделяет строки окна и кнопки в его ряду: это
расстояние между контролами, и оно одно на всё окно. В вертикальном списке
вариантов было то же самое — поправлено заодно.

Тест на зазор и строка в docs/widgets.md про то, чем эти две метрики
отличаются: перепутать их легко, я и перепутал.

## v0.0.11 — Прокрутка перестала дрожать и отскакивать у краёв файла

*20 августа 2026 года*

Живая проверка показала две беды подряд: приложение вставало наглухо после
нескольких PgUp/PgDn, а у начала файла появлялся отскок. Тестами ни то, ни
другое не воспроизводилось — обе поймали снятием стека и печатью из живого
процесса через службу VM.

Корень один и тот же, и он же был причиной разрывов в выделении: сетку строк
библиотека меряет **в двух местах**, и оба меряли без струны, которой строки
раскладываются. Первое место починено раньше, второе — у самого рендера
(_calculatePreferredLineHeight) — нашлось только теперь: 21 точка против
настоящих 18. Три точки на строку копились в поправку к положению прокрутки,
та уводила его выше начала файла, и физика возвращала его пружиной.

Заодно, по дороге к причине, в копии появились предохранители: предел циклов
раскладки (рекурсия не сходилась, в стеке были тысячи кадров одного метода),
прыжок к строке в границах диапазона и от последнего видимого абзаца, а не от
первого, и отказ пересобирать строки выше, когда выше уже ничего нет.

Курсор в просмотрщике скрыт: править нечего, а мигающая палочка обещает ввод.
Позицию поле держит по-прежнему — ею листают стрелки и страницы.

Замер сетки прогоняется теперь по всем шрифтам проекта: согласованность от
шрифта не зависит, и тест это фиксирует. Зависит только выбранное число 1.6 —
им сохранён прежний интервал Consolas.

## v0.0.10 — Замер открытия каталога целиком: 82 мс на десять тысяч записей

*20 августа 2026 года*

К замерам чтения и сортировки добавлен третий — тот путь, который проходит
панель по Enter: чтение в изоляте плюс постройка узлов в главном. Узлы
строятся именно там, и иначе нельзя: они держат ссылки на провайдера и на
родителя, а такие связи через границу изолята не переносят.

    записей   открытие
       1000    10.49 мс
      10000    82.25 мс

Разбор оставшегося времени показал, что оптимизировать в чтении больше
нечего: на десяти тысячах записей перечисление каталога стоит 22.8 мс,
`statSync` на каждую запись — ещё 50 мс, и это 73 из наших 74.5. Всё
остальное — имя из пути, строка атрибутов, сама запись — уже теряется в
шуме. Дальше пришлось бы отказываться от `stat` на запись, а его не
избегает и `ls -l`.

В описании замера теперь сказано и про сравнимость чисел: виртуальная
машина разогревается по ходу прогона, и та же сортировка в конце идёт
втрое быстрее, чем в начале. «До» и «после» имеет смысл мерить одной и той
же командой — иначе ускорение легко приписать себе.

## v0.0.9 — Буфер обмена в API и выделение текста в просмотрщике

*20 августа 2026 года*

Текст в просмотрщике можно выделить мышью и скопировать — `Cmd-C`, как
везде. Выделение обычное текстовое (`SelectionArea`), а копирует его команда
экрана: клавиша принадлежит просмотрщику, подпись видна в ряду кнопок, и
пока ничего не выделено, она приглушена — а не делает вид, что сработала.

Под этим — `ClipboardService` в API. Не прямой вызов Flutter из
просмотрщика: буфером будут пользоваться многие (панель — «скопировать
путь» и «список имён»), а в тестах он обязан быть подставным. Прогон,
трогающий настоящий буфер машины, незаметно стирает то, что человек за ней
только что скопировал, — поэтому подставка в `TestPlatform` стоит по
умолчанию, а не по просьбе.

Реализация — в модуле локальной файловой системы, рядом с остальным
платформенным: API объявляет, что буфер есть, и не знает, чем он сделан.

## v0.0.8 — Файловая система по SSH: панель встаёт на сервер целиком

*19 августа 2026 года*

Первый модуль с чужим корневым провайдером. Всё, что для него нужно,
стояло без потребителя с прошлого шага: AddressFactory, addressProvider,
панель, забирающая чужой корень себе, общий канал секретов — в его
документации ssh:user@example.org стоял прямо как пример.

Провайдер поверх SFTP (dartssh2): дерево, копирование в обе стороны,
mkdir, удаление, переименование — теми же командами, что и локально.
Ядро не тронуто ничем, кроме одной правки ниже: движок переноса работает
с сервером через те же примитивы, что и с диском.

Библиотека спрятана за узким SftpApi, как 7-Zip за ProcessRunner: сам
провайдер про dartssh2 не знает, поэтому проверяется без сервера, а
ошибки протокола выходят наружу уже переведёнными в FsError — кода «уже
существует» в третьей версии SFTP нет, и занятое имя проверяется до
действия, а не по ответу.

Путей два вида, а не три. У локального провайдера есть physicalPathOf и
entityPathOf, потому что он сам склеивает пути через ссылки; здесь их
разворачивает сервер, а последнюю ссылку в пути он при удалении и
переименовании не трогает — то есть ровно нужное поведение. Осталась
одна функция.

Вход: сперва ключи из ~/.ssh, открывшиеся без фразы, — молча и без
обработчика пароля; если сервер их не принял, второй заход уже с
вопросами (парольная фраза, потом пароль). Так сервер, пускающий по
открытому ключу, не спрашивает ничего. Ключ хоста пока принимается без
проверки — known_hosts это отдельная работа со своим окном, и сделанная
наполовину она хуже несделанной; это записано в коде долгом.

SftpFile.write не используется: он пишет с опережением, но ошибку роняет
мимо возвращённого будущего, и обрыв на середине выглядел бы как вечное
копирование. Пишем по куску с ожиданием — медленнее, зато ошибка
приходит туда, где её разбирают.

Правка в ядре одна: Panel.openPath получил allowConnect. Восстановление
состояния при запуске в сеть ходить не должно — иначе каждый запуск
начинался бы с вопроса о пароле поверх ещё пустых панелей, а недоступный
сервер задерживал бы его до истечения времени подключения. На сервер
человек возвращается сам, как в Total Commander и Far.

Проверено не только подставкой: живой тест ходит на настоящий сервер
(FC_SSH_TEST_HOST, иначе localhost и пропуск, как у 7-Zip) — вход по
ключу без единого вопроса, листинг со ссылками и правами, 256 КБ туда и
обратно с посимвольной сверкой, чтение с середины, отказы, и панель,
встающая на сервер и уходящая с него. Заодно живьём проверен архив на
сервере, открытый показанным путём: этот стык был заявлен модулем zip,
но никогда не работал по-настоящему.

## v0.0.7 — Ответ по вводу: протокол, путь, доступ или «не то»

*19 августа 2026 года*

«Blah» отвечало «не поддерживается», хотя это ни путь, ни протокол.
Теперь ввод разбирается честно: строка с протоколом — про протокол,
строка без него обязана быть путём (с «/» или «~»), а всё остальное —
«Wrong URI». Не найдено значит, что искали; про «Blah» искать было
нечего.

В словарь ошибок добавлены два вида: unsupportedScheme («Protocol ssh
is not supported», где путь — имя протокола) и invalidAddress («Wrong
URI»). Отдельно от notSupported: тот про то, чего не умеет источник, а
эти — про то, что открывать нечем и нечего.

И главное: неудача больше не стирает панель. Прошлая правка ставила
статус ошибки, а таблица в этом состоянии подменяется сообщением —
получалось, что за опечатку в адресе человек терял то, на что смотрел.
Панель меняется только после успешного открытия, причина уходит в окно
ввода.

## v0.0.6 — Открытие произвольного адреса в панели

*19 августа 2026 года*

Cmd+F1 и Cmd+F2 открывают окно с полем пути над левой или правой
панелью — то, чем в Norton Commander был выбор диска. Дисков в этом
смысле в macOS нет, а источников будет много, поэтому спрашивается
строка целиком: /etc, ~/Downloads, а со следующего шага и
ssh://user@host/srv.

Под этим — то, ради чего всё затевалось: у панели появился **свой
корень**. Архив встраивается звеном в путь, сервер начинает путь, и
разбор это теперь различает: чужая схема в начале означает другой
корень, а не отказ. Источник по адресу объявляется модулем
(addressProvider), принадлежит открывшей его панели и закрывается,
когда она уходит; общий корень не закрывается никогда.

Путь без схемы — всегда общий корень: стоя на сервере, домой
возвращаются обычным путём. Тот же адрес второй раз не подключается —
второе подключение разошлось бы состоянием с первым.

Отмена охватывает и подключение: раньше операция заводилась только на
чтении каталога, и пока панель подключалась, Esc было нечего отменять.
Для сервера на другом конце света это ровно то окно, в котором отмена и
нужна.

Команда работает с названной панелью, а не с активной, — единственное
такое место, — и поэтому её окно встаёт над своей панелью.

## v0.0.5 — Курсор возвращается туда, где его оставили

*19 августа 2026 года*

Панель и раньше помнила курсор для каждого каталога, но только в памяти:
после перезапуска он падал в начало списка. Теперь имя объекта под
курсором лежит в настройках панели.

Имя, а не номер строки: за время между запусками в каталоге прибавится
или убавится файлов, и номер привёл бы не туда. Пропавшее имя ставит
курсор в начало — честнее, чем угадывать соседа.

Отдельной ветки восстановление не имеет: прочитанное кладётся в ту же
память курсоров, которой панель пользуется при обычном хождении, и
открытие сохранённого каталога проходит тем же путём, что возврат в уже
посещённый. В файле — одна запись на панель: хранить всю карту значило
бы копить список каталогов, где пользователь когда-либо был.

Движение курсора по-прежнему не вызывает записи на диск — иначе каждый
шаг стрелкой заводил бы таймер. Планирование отложенной записи
сравнивает снимок без курсора, сама запись — полный: курсор уезжает в
файл вместе со следующей причиной записать и при выходе.

## v0.0.4 — Наружу не выпускается только Escape

*19 августа 2026 года*

Прошлая починка съедала любое событие без команды — и заодно забрала у
системы Cmd+Q. Причина в том, что моё объяснение было неверным: меню
разбирает сочетания не раньше Flutter, а позже. performKeyEquivalent:
идёт к фреймворку первым, и «обработано» отменяет пункт меню.

Круг с Escape рвётся и более узким правилом: AppKit отвечает на Cmd+.
одним Escape, и достаточно не выпускать наружу его. Одно нажатие теперь
даёт одно событие вместо семнадцати тысяч, а Cmd+Q снова закрывает
приложение — проверено на живом приложении, с трассировкой и с
подтверждением, что нажатие уходит именно в наше окно.

Тест на «клавиша без команды дальше не идёт» закреплял ровно ту ошибку,
из-за которой пропал Cmd+Q, — заменён на обратный.

## v0.0.3 — Клавиша без команды не уходит в систему

*19 августа 2026 года*

Cmd+. macOS понимает как «отменить операцию»: не найдя команды, мы
отдавали событие дальше в AppKit, тот присылал в ответ Escape, наш
обработчик и его не тратил — и событие уходило обратно. Одно нажатие
давало больше тридцати тысяч событий Escape подряд.

Пока шёл этот поток, чтение каталога отменялось первым же из них:
Enter выглядел как «ничего не произошло» — ровно столько раз, сколько
было нажатий Cmd+.

Теперь событие всегда считается обработанным. Терять нечего: клавиши
окна принадлежат приложению целиком, а сочетания меню (Cmd+Q, Cmd+W,
Cmd+M) сюда и не доходят — их разбирает строка меню раньше Flutter.
Проверено трассировкой живого приложения.

## v0.0.2 — Сборка без вырезки глифов

*19 августа 2026 года*

DefaultIcons строит IconData не константой: семейство шрифта приходит
из темы во время работы, поэтому tree shaking иконок роняет релизную
сборку. Отключаем его в обоих workflow.

## v0.0.1 — Считающий поток: ход виден и внутри записи

*19 августа 2026 года*

Упаковщику подсовывается CountingInputStream, и байты приходят по мере того,
как он читает запись, — бар текущего объекта движется, а не стоит до конца
файла.

Запись при этом читается дважды: сперва ради контрольной суммы, потом ради
сжатия. Считать только первый проход заманчиво — объём совпал бы с размером
файла, — но тогда полоса заполнялась бы на быстром чтении и замирала на
медленном сжатии, то есть ровно там, где движение и нужно. Поэтому в работу
идут оба прохода, а объём записи в счёте удвоен.
