# Палитра команд

**Состояние:** сделано, выпуск `v0.0.20`; страницы и описания — этап А9.
Отметки по ходу — в §10.

Этап А4. Список всего, что приложение умеет **сейчас**, с поиском по названию и
клавишами напротив каждой строки. `Cmd-Shift-P`.

## 1. Зачем, кроме удобства

Команда, которой не досталось клавиши, сегодня невидима и невызываема. Так уже
случилось: `terminal.toggleTyping` переключает режим `mc`, значится в справке —
и вызвать его нечем. Это не единичный случай, а свойство устройства: клавиш
меньше, чем команд, и с каждым модулем разрыв растёт.

Палитра закрывает его целиком и навсегда: **всё, что объявлено командой,
запускается**. Отсюда же следует, что заводить команду становится дёшево —
можно не думать, где взять для неё клавишу.

## 2. Что показано

**Только то, что можно сделать прямо сейчас.** Невыполнимые команды в список не
попадают: палитра — это ответ на вопрос «что мне доступно», а не каталог
возможностей. Приглушённая строка, на которую нельзя нажать, здесь только
мешает — в отличие от ряда кнопок, где приглушённая кнопка держит своё место в
раскладке.

Полный перечень, включая невыполнимое, остаётся в справке (`F1`) — там он и
уместен: справка отвечает на вопрос «что вообще бывает».

Строка показывает три вещи:

| Что | Откуда | Зачем |
|---|---|---|
| название | `AppCommand.label` | по нему и ищут |
| описание | `AppCommand.description` | единственный вопрос, ради которого заходят с полузнакомой командой |
| клавиши | `bindingsOf` | заодно и учит: увидел раз — дальше жмёшь клавишу |

**Описание, а не модуль** (А9). Первая версия ставила за названием модуль
(`CommandRegistry.ownerOf`) и объявляла, что описание в строку не выносится:
оно длинное, а строк много. Отменено вместе с причиной — описание это **одно
короткое предложение**, длинное режется многоточием и целиком остаётся в
справке. Строка не растёт: как была в один ряд, так и остаётся. А модуль
сообщал ровно то, что и так видно по названию, — и на вопрос «что эта команда
делает» не отвечал.

Третьей приглушённой части в строке нет: справа уже стоят клавиши. **Пустое
описание — пустое место**: у «Cursor up» объяснять нечего
(`AppCommand.description` это прямо разрешает), и подставлять туда модуль ради
заполненной колонки нельзя — в одной колонке оказались бы две разные вещи.
Заодно видно, у каких команд описания нет: это и есть список того, что
дописать.

Модуль при этом никуда не делся — он переехал в невидимые признаки, к
синонимам (§3).

## 3. Поиск

Нечёткий, как в палитре VS Code: буквы запроса ищутся **по порядку, но не
подряд** — `cpf` находит `Copy File`. Регистр не важен.

Вес совпадения складывается из трёх вещей, и порядок между ними важнее самих
чисел:

1. **С начала слова** весит больше, чем с середины: `set` в `Settings` лучше,
   чем в `Reset`.
2. **Подряд** весит больше, чем вразбивку: `copy` в `Copy` лучше, чем `c…o…p…y`
   по разным словам.
3. **Короткое название** при равном весе выигрывает: `Copy` перед
   `Copy path to clipboard`.

Совпавшие буквы подсвечены — иначе непонятно, почему строка вообще нашлась.

Ищется по названию **и по синонимам** (`AppCommand.keywords`) — словам, которых
в названии нет, а искать по ним будут. Первым таким случаем стала `Mk Tar`: она
умеет `.tar.gz`, а набравший `gz` не находил её вовсе. Растягивать ради этого
название нельзя — оно стоит в ряду кнопок и в заголовке окна. Синонимы весят
вдвое меньше названия: название всё же ближе к делу. В строке их не видно и
подсвечивать в них нечего.

**Название модуля — такой же синоним** (А9): `term` по-прежнему находит команды
терминала, даже если в их названиях этого слова нет. Раньше у модуля был свой
разряд в отборе — со своим весом (тот же, вдвое меньше названия) и своей
подсветкой в строке. Теперь его в строке не видно, подсвечивать нечего, и
отдельный разряд ради одного и того же веса не нужен: палитра просто дописывает
модуль к синонимам команды.

**Описание не ищется.** Предложение состоит по большей части из общих слов, и
поиск по нему выдавал бы всё сразу на любом «file» или «panel». Для «нашлось не
по названию» есть синонимы. Оно и не подсвечивается — подсвечивать в нём
нечего.

Синонимы — не описание: описание объясняет дело предложением, а здесь короткие
слова, которые придут в голову вместо названия.

Два правила держат список от разрастания, и оба проверяются тестом:

1. **Слово, которое и так находится по названию, — мёртвый груз.** `mv` у
   «Move» находится и без синонимов (`m…v` по буквам), а `split` — у «Center
   split» просто есть в названии. Такие в список не попадают: они не добавляют
   ничего, зато создают вид работы.
2. **Не заводить синоним на то, чего команда не делает.** `rename` к «Move» не
   приписан, хотя в коммандерах `F6` переименовывает: приёмником здесь может
   быть только каталог, другого имени команде не задать. Привести человека по
   этому слову — значит дать ему найти команду и не суметь сделать искомое.

Из синонимов в счёт идёт **лучший**, а не первый совпавший: порядок в
объявлении — не мера того, насколько слово подошло.

## 4. Недавние

Последние десять запусков, живут между запусками приложения (раздел `fc.shell`).
При пустом запросе они идут первыми — обычно человек открывает палитру ради
того же, что делал вчера.

При непустом запросе недавность — последний довод при равном весе, а не
самостоятельная сила: искали конкретное, а не привычное.

## 5. Клавиши

| Клавиша | Что делает |
|---|---|
| `Cmd-Shift-P` | открыть палитру |
| печать | отбор по набранному |
| `↑` / `↓` | по списку |
| `PgUp` / `PgDn` | по списку страницами |
| `Enter` | выполнить выбранное |
| `Esc` | закрыть |

`F2` палитре, вопреки плану, не достаётся: он занят настройками. Иерархического
меню не будет — реестр команд и есть меню, а заглушка `app.menu` уже убрана.

Стрелки, страницы и `Enter` разбирает сам список: пока открыто окно, клавиши
принадлежат ему целиком (`keyboard.md`), и поле ввода их не получает. С панелью
`PgUp`/`PgDn` не спорят по той же причине.

**Страница — это видимые строки минус одна**, как в панели
(`panel.cursor.pageUp`): перекрытие в одну строку не даёт потерять место, где
остановился взгляд.

**У края — упор, а не заворот.** Стрелки заворачиваются по кругу (и вверх с
первой строки дают -1, чтобы вернуть набранное), но страница на этом же правиле
означала бы прыжок с конца в начало от одного нажатия. `PgUp` на первой
странице ставит на первую строку, `PgDn` на последней — на последнюю.

**Страницы живут в списке, а не в палитре** (`FcPickList.moveSelection`), и
достаются заодно истории адресов и окну масок: списки одинаковые, и расходиться
повадкой им незачем. Сколько строк видно, знает при этом сам список — обзор
прокрутки, делённый на высоту строки, — и кладёт это число в записку
(`FcPickPage`), которую вызывающий отдаёт разбору клавиш. Устроено это как шаг
страницы в панели: видимые строки выставляет таблица (`panel.pageSize`), а
пользуется ими команда.

`Home` и `End` в список пока не заведены. Понадобятся — заведутся там же, в
`moveSelection`, и отдельного разговора не потребуют.

**Кнопок внизу нет вовсе** — ни «Close», ни «Run», как в палитре VS Code. Это не
окно с формой, которую заполняют и подтверждают, а поиск: набрал, выбрал, нажал.
«Close» повторяла бы `Esc`, «Run» — `Enter`, и обе отнимали бы у списка строку,
ради которой окно и открывают. Единственное окно приложения без ряда кнопок, и
это не исключение из правила, а другой род окна.

Отступ снизу при этом остаётся — такой же, каким поле отбито от полосы
заголовка: без него окно кончалось бы строкой списка впритык к краю.

**Ширина окна — три четверти экрана**, а не половина, как у остальных окон
(`paletteWidthFactor` против `dialogWidthFactor`). Палитра это список, и в
строке у неё название, описание и клавиши разом: в половине экрана описание
обрывалось бы многоточием у большинства команд. У окна с формой такой нужды
нет — там поля, а не строки.

Верхний предел рамы палитре при этом не указ (`DialogSpec.ownWidth`): он задан
в точках (`dialogMaxWidth`, 800), и на широком экране обрезал бы долю тем
сильнее, чем экран шире — на 1512 точках вышло бы 800 вместо 1134. Своего
предела хватает: он в тех же долях экрана (`dialogMaxScreenFactor`), и три
четверти — ровно он.

**Окно стоит под верхним краем** — на `dialogTopInset` (130) от него, — и с
этого места не сходит (`DialogPlacement.top`). Посередине, как прочие окна, ему
нельзя: находок по ходу набора то больше, то меньше, окно растёт и сжимается на
каждую букву, а растущее вокруг своей середины двигает **обе** свои границы —
вместе с полем ввода, в которое человек в этот момент и смотрит. Прибитое к
верху, оно двигает только нижнюю.

Оттуда же меряется и его высота (`dialogContentLimits(topInset:)`): сверху у
палитры не поле, а назначенное место, и окно, переросшее предел, раскладка
подняла бы выше него — то самое дрожание, ради избавления от которого его туда
и прибили.

**Высота окна — целое число строк.** Окно тянется во всю высоту экрана, и
остаток от деления резал бы нижнюю строку пополам: список выглядел бы обрезанным
ровно там, где взгляд ищет его конец. Предел округляется вниз до целой строки, и
считает его само окно, а не список: узнать доставшуюся высоту изнутри можно
только `LayoutBuilder`, а он не умеет отвечать на вопрос о собственной ширине —
тот самый, который рама задаёт каждому окну (`IntrinsicWidth`).

## 6. Запуск — тем же путём, что и клавиша

`CommandService.run(id)` — тот же вызов, что делает нажатие клавиши и кнопка
внизу окна. Ничего своего палитра не делает: иначе однажды окажется, что
команда, запущенная из палитры, ведёт себя не так, как та же команда с клавиши.

Палитра закрывается **до** запуска: у команды может быть своё окно, и открывать
его поверх палитры незачем.

Контекст при этом не меняется: окно не меняет ни активную область, ни
панель-источник (`viewport.dart`), поэтому команда видит ту же панель, что
увидела бы с клавиши.

## 7. Где живёт

* `fc_ui_kit` — окно со списком (`FcCommandPalette`): поле ввода, строки,
  подсветка совпадений.
* Оболочка (`lib/modules/app_shell.dart`) — команда `app.commands` и привязка.
* Отбор и веса — чистая функция рядом с окном: у неё больше всего случаев, и
  проверять её надо без приложения.

## 8. Чего в первой версии нет

* **Значений для команд.** Палитра запускает команду без параметров — как
  клавиша без них. «Открыть путь в правой панели» останется отдельной командой.
* ~~**Своих названий у команд под поиск** (алиасов): `label` и модуль
  довольно.~~ **Оказалось недовольно** — см. §3 и отметку в §10.
* **Истории запусков с параметрами** и повторения последнего.
* **Показа невыполнимых** — см. §2.

## 9. Проверка

Чистые тесты отбора: `cpf` находит `Copy File`; совпадение с начала слова выше,
чем из середины; подряд выше, чем вразбивку; при равном весе выигрывает короткое
название; синоним находит команду, которой этих букв в названии нет, весит
меньше названия, ничего не подсвечивает и в счёт идёт лучшим из списка; модуль
стоит среди синонимов и спорит с ними на равных; пустой запрос отдаёт всё.

Чистые тесты хода по списку (`pick_list_test.dart`): страница вниз двигает на
видимые строки минус одну, у нижнего края встаёт на последнюю строку и не
заворачивается, `PgUp` с первой страницы даёт первую строку, на пустом списке
обе клавиши молчат, из поля вниз страницей входят в список, а вверх идти
некуда, до первой раскладки страница равна строке.

Сторож на весь набор синонимов сразу (`test/view/command_palette_test.dart`):
ни один не находится по названию без него, все записаны строчными, упаковщики
отзываются на `compress` и `archive`, «Mk Dir» — на `folder`, «Settings» — на
`preferences`, а `rename` не приводит к «Move».

Виджет-тесты: `Cmd-Shift-P` открывает; набор отбирает строки; `↑`/`↓` двигают
выбор; `PgDn` двигает на видимые строки минус одну, а `PgUp` у верхнего края
упирается в первую; рядом с названием стоит описание, а не модуль; команда без
описания показывается одним названием; по названию модуля команда по-прежнему
находится; `Enter` запускает и закрывает; команда с окном показывает своё окно;
`Esc` закрывает; невыполнимой команды в списке нет; недавние идут первыми и
переживают перезапуск; окно занимает три четверти ширины — и на широком экране
тоже, где предел рамы в точках обрезал бы долю.

Золотой снимок палитры — `test/view/goldens/command_palette.png`.

## 10. Отметки по ходу

**Сделано.** Отбор (`palette_search.dart`), окно (`command_palette.dart`),
команда `app.commands` в оболочке, раздел `fc.shell` под недавние.

**Веса пришлось переписать после первого же теста.** Правило «с начала слова»
перебивало правило «подряд»: `opy` находилось в `Order picked yesterday` лучше,
чем в `Copy`, — три начала слова весят больше двух букв подряд. Не хватало
третьего правила: **штрафа за разрывы**. С ним порядок встал на место, а сами
числа взяты по образцу `fzf` — важны не они, а порядок между правилами.

**Список — не ленивый.** Рама окна меряет содержимое (`IntrinsicWidth`), а
`ListView` на такой вопрос отвечать не умеет и падает с «does not support
returning intrinsic dimensions». Команд полсотни, лень тут и не нужна.

**Список и строка выбора — общие с историей адресов** (`FcPickList`,
`spec/address-history.md`). Подсветка идёт до краёв окна, а текст в ней стоит
ровно под набранным в поле.

**Клавиши списка разбираются на самом поле ввода** (`FocusNode.onKeyEvent`):
стрелки и `Enter` иначе достались бы полю — оно двигает ими курсор и
подтверждает ввод.

**Что проверено:** тесты отбора (порядок букв, регистр, начало слова против
середины, подряд против вразбивки, короткое название, поиск по модулю,
синонимы, подсветка, пустой запрос) и семь на окно (`Cmd-Shift-P`, отбор,
невыполнимого нет, `Enter` запускает и закрывает, стрелки двигают выбор, `Esc`,
недавние первыми и переживают перезапуск).

**А9: страницы и описания.** Две правки к сделанной палитре — она стала главным
способом дотянуться до редкой команды, и в этой роли ей не хватало быстрого
хода по длинному списку и ответа на вопрос «а что эта команда делает».
`PgUp`/`PgDn` легли в `FcPickList.moveSelection` и достались заодно истории
адресов и окну масок. Описание встало на место модуля, а модуль уехал к
синонимам — и вместе с ним из отбора ушёл целый разряд: `owner` и `ownerHits`
у `matchCommand` держались только ради подсветки в строке, а подсвечивать в
невидимом нечего.

**Палитра расширена до трёх четвертей экрана.** С описанием в строке половины
стало мало: название, описание и клавиши разом в половине экрана не помещались,
и описание обрывалось многоточием у большинства команд. Заодно выяснилось, что
доля упирается в предел рамы, заданный в точках, — пришлось завести
`DialogSpec.ownWidth` для окон, назначающих ширину самим.

**Сколько строк видно, пришлось спрашивать у списка.** Высоту ему даёт рама
окна, и до первой раскладки её не знает никто, а разбор клавиш живёт снаружи —
на поле ввода. `LayoutBuilder` внутри списка исключён по той же причине, по
какой окно само считает свою высоту (§5): он не отвечает на вопрос о
собственной ширине. Обошлось обзором прокрутки и запиской на двоих
(`FcPickPage`) — ровно как `panel.pageSize` у панели.

**Отложенное вернулось: синонимы понадобились.** §8 объявлял, что `label` и
модуля довольно, — и это продержалось до `Mk Tar`. Разошлись они и у полутора
десятков команд помимо архиваторов: `folder` у «Mk Dir», `preferences` у
«Settings», `shortcuts` у «Help», `refresh` у «Reload», `dotfiles` у «Hidden
files», `dark` у «Switch theme», `shell` у «Terminal». Команда умеет `.tar.gz`,
человек ищет `gz` и не находит ничего: в названии этих букв нет, а модуль
зовётся «Tar archives». Решение оказалось на строчку — `keywords` у команды,
которые отбор спрашивает после названия и перед модулем. Отложено было верно:
на двух архиваторах нужда не видна, а придумай мы синонимы заранее — заполняли
бы их у всех команд подряд, где они не нужны.
