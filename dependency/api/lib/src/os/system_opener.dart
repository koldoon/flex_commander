/// Открыть путь тем, чем его открывает система.
///
/// Здесь только контракт: сама операция платформенная («open», «xdg-open»,
/// «cmd /c start»), и её реализация — дело модуля локальной файловой системы.
typedef SystemOpener = Future<void> Function(String path);
