/// Оболочка в том же окне: то, что живёт в ядре.
///
/// Одна вещь — чем запускать оболочку: спрашивает её локальная файловая
/// система, а выбирает человек в настройках терминала.
library;

export 'src/terminal_backend.dart';
export 'src/terminal_settings.dart';
